/**
 * SUPERSEDED — this file is no longer the canonical spec.
 *
 * The current Playwright specs live at:
 *   ../.playwright/scripts/flow-1-chunked-upload.spec.ts
 *   ../.playwright/scripts/flow-2-progress.spec.ts
 *   ../.playwright/scripts/flow-3-resume.spec.ts
 *
 * Run from the .playwright/ directory:
 *   cd ../.playwright && npm install && npx playwright test
 */

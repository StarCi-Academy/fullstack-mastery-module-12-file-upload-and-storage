/**
 * Redirect — the canonical Playwright config for this lesson lives at
 * `../.playwright/playwright.config.ts` (lesson root, not inside frontend/).
 *
 * Run tests from the .playwright/ directory:
 *   cd ../.playwright && npm install && npx playwright test
 *
 * Or from the frontend/:
 *   npm run test:e2e
 */
export { default } from "../.playwright/playwright.config"
